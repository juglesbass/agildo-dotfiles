# ===== SHELL: Waybar (config propria em ~/.config/waybar) ================
# Ativado por: switch-shell.sh waybar
# O waybar nao tem daemon de notificacoes: o mako entra junto.

exec-once = waybar
exec-once = mako

bind = $mainMod SHIFT, B, exec, /home/agildo/.local/bin/restart-waybar
